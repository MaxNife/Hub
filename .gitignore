# build output
# Anchored to the repo root: a bare `hub` also matched cmd/hub/ (the entrypoint).
/hub.exe
/hub
data/hub.db*
data/*.log
data/hub.log.*
data/backups/
web/dist/
web/node_modules/

# local Go toolchain (downloaded, not committed)
.tools/

# Go
__debug_bin*

# Windows
Thumbs.db
